[package]
name = "x402pp"
version = "0.1.0"
description = "Gist Plus Solana Program - On-chain receipts, sessions, and escrow for AI commerce"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "x402pp"

[features]
no-entrypoint = []
no-idl = []
no-log-ix-name = []
cpi = ["no-entrypoint"]
default = []

[dependencies]
anchor-lang = "0.29.0"
anchor-spl = "0.29.0"

